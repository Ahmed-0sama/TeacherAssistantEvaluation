<div class="card shadow-sm mx-auto" style="max-width: 1100px;">
    <div class="card-body p-4">
        <h5 class="text-end text-dark mb-4">رابعاً: الأنشطة الطلابية</h5>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(12)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط رياضي</label>
                @if (IsDeanModified(12))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(12, isStudentActivity: true)"
                        @onchange="e => UpdateRating(12, e.Value?.ToString(), isStudentActivity: true)"
                        disabled="@(IsViewMode || IsDeanModified(12))">
                    <option value="">اختر</option>
                    @foreach (var option in studentActivitiesGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(13)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط اجتماعي</label>
                @if (IsDeanModified(13))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(13, isStudentActivity: true)"
                        @onchange="e => UpdateRating(13, e.Value?.ToString(), isStudentActivity: true)"
                        disabled="@(IsViewMode || IsDeanModified(13))">
                    <option value="">اختر</option>
                    @foreach (var option in studentActivitiesGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(14)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط ثقافي</label>
                @if (IsDeanModified(14))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(14, isStudentActivity: true)"
                        @onchange="e => UpdateRating(14, e.Value?.ToString(), isStudentActivity: true)"
                        disabled="@(IsViewMode || IsDeanModified(14))">
                    <option value="">اختر</option>
                    @foreach (var option in studentActivitiesGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="border-top pt-3">
            <p class="text-end fw-semibold mb-0">
                إجمالي تقييم الأنشطة الطلابية:
                @CalculateStudentActivitiesTotal() / 10
            </p>
        </div>
    </div>
</div>

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">
            <h5 class="card-title text-primary fw-bold">
                خامساً: استقصاء الطلاب (النتيجة تجلب تلقائياً)
            </h5>
            <div class="mt-3 p-3 border rounded bg-light d-flex justify-content-between align-items-center">
                <span class="fw-semibold">النتيجة النهائية:</span>
                <span class="badge bg-success fs-6">4 / 5</span>
            </div>
        </div>
    </div>
</div>

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">
            <h5 class="card-title text-primary fw-bold">
                سادسا: الارشاد الاكاديمي (النتيجة تجلب تلقائياً)
            </h5>
            <div class="mt-3 p-3 border rounded bg-light d-flex justify-content-between align-items-center">
                <span class="fw-semibold">النتيجة النهائية:</span>
                <span class="badge bg-success fs-6">4 / 5</span>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm mx-auto mb-4 mt-4" style="max-width: 1100px;">
    <div class="card-body p-4">
        <h5 class="text-end text-dark mb-4">السمات الشخصية</h5>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(15)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">التعاون والعمل الجماعي</label>
                @if (IsDeanModified(15))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(15, isStudentActivity: false)"
                        @onchange="e => UpdateRating(15, e.Value?.ToString(), isStudentActivity: false)"
                        disabled="@(IsViewMode || IsDeanModified(15))">
                    <option value="">اختر</option>
                    @foreach (var option in personalTraitsGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(16)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">الالتزام بالمواعيد</label>
                @if (IsDeanModified(16))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(16, isStudentActivity: false)"
                        @onchange="e => UpdateRating(16, e.Value?.ToString(), isStudentActivity: false)"
                        disabled="@(IsViewMode || IsDeanModified(16))">
                    <option value="">اختر</option>
                    @foreach (var option in personalTraitsGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(17)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">المظهر العام</label>
                @if (IsDeanModified(17))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(17, isStudentActivity: false)"
                        @onchange="e => UpdateRating(17, e.Value?.ToString(), isStudentActivity: false)"
                        disabled="@(IsViewMode || IsDeanModified(17))">
                    <option value="">اختر</option>
                    @foreach (var option in personalTraitsGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(18)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">المبادرة و تحمل المسؤولية</label>
                @if (IsDeanModified(18))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(18, isStudentActivity: false)"
                        @onchange="e => UpdateRating(18, e.Value?.ToString(), isStudentActivity: false)"
                        disabled="@(IsViewMode || IsDeanModified(18))">
                    <option value="">اختر</option>
                    @foreach (var option in personalTraitsGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center @GetDeanModifiedClass(19)">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">إدارة الوقت</label>
                @if (IsDeanModified(19))
                {
                    <span class="badge bg-warning text-dark ms-2">تم التعديل من قبل العميد</span>
                }
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(19, isStudentActivity: false)"
                        @onchange="e => UpdateRating(19, e.Value?.ToString(), isStudentActivity: false)"
                        disabled="@(IsViewMode || IsDeanModified(19))">
                    <option value="">اختر</option>
                    @foreach (var option in personalTraitsGradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="border-top pt-3">
            <p class="text-end fw-semibold mb-0">
                إجمالي تقييم السمات الشخصية:
                @CalculatePersonalTraitsTotal() / 10
            </p>
        </div>
    </div>
</div>

<style>
    .dean-modified-row {
        background-color: #fff3cd;
        border-left: 4px solid #ffc107;
        padding: 10px;
        border-radius: 4px;
    }
</style>

@code {
    [Parameter] public EventCallback<Dictionary<int, int>> OnPersonalTraitsChanged { get; set; }
    [Parameter] public EventCallback<Dictionary<int, int>> OnStudentActivitiesChanged { get; set; }
    [Parameter] public bool IsViewMode { get; set; }
    [Parameter] public Dictionary<int, int>? ExistingPersonalTraits { get; set; }
    [Parameter] public Dictionary<int, int>? ExistingStudentActivities { get; set; }
    [Parameter] public Dictionary<int, string>? PersonalTraitsSources { get; set; }
    [Parameter] public Dictionary<int, string>? StudentActivitiesSources { get; set; }

    private Dictionary<int, int> personalTraitsRatings = new();
    private Dictionary<int, int> studentActivitiesRatings = new();

    // ✅ FIXED: Track previous values to detect changes
    private Dictionary<int, int>? previousPersonalTraits = null;
    private Dictionary<int, int>? previousStudentActivities = null;

    // Personal Traits (5 criteria × 2 points = 10 points)
    private List<GradeOption> personalTraitsGradeOptions = new()
    {
        new GradeOption { Value = 0, Label = "ضعيف (0)" },
        new GradeOption { Value = 1, Label = "مقبول (0.5)" },
        new GradeOption { Value = 2, Label = "جيد (1)" },
        new GradeOption { Value = 3, Label = "جيد جداً (1.5)" },
        new GradeOption { Value = 4, Label = "ممتاز (2)" }
    };

    // Student Activities (3 criteria, scaled to 10 points)
    private List<GradeOption> studentActivitiesGradeOptions = new()
    {
        new GradeOption { Value = 0, Label = "ضعيف (0)" },
        new GradeOption { Value = 1, Label = "مقبول (1)" },
        new GradeOption { Value = 2, Label = "جيد (2)" },
        new GradeOption { Value = 3, Label = "جيد جداً (3)" },
        new GradeOption { Value = 4, Label = "ممتاز (3.33)" }
    };

    private bool IsDeanModified(int criterionId)
    {
        // Check student activities (12-14)
        if (criterionId >= 12 && criterionId <= 14)
        {
            return StudentActivitiesSources?.TryGetValue(criterionId, out var source) == true && source == "Dean";
        }
        // Check personal traits (15-19)
        else if (criterionId >= 15 && criterionId <= 19)
        {
            return PersonalTraitsSources?.TryGetValue(criterionId, out var source) == true && source == "Dean";
        }
        return false;
    }

    private string GetDeanModifiedClass(int criterionId)
    {
        return IsDeanModified(criterionId) ? "dean-modified-row" : "";
    }

    protected override void OnParametersSet()
    {
        // ✅ FIXED: Only reload Personal Traits if they changed
        if (ExistingPersonalTraits != null && !AreRatingsDictionariesEqual(ExistingPersonalTraits, previousPersonalTraits))
        {
            Console.WriteLine($"🔄 Loading {ExistingPersonalTraits.Count} existing Personal Traits ratings");

            personalTraitsRatings = new Dictionary<int, int>();
            foreach (var kvp in ExistingPersonalTraits)
            {
                // RatingId 11-15 maps to ScoreValue 0-4
                int scoreValue = ConvertRatingIdToScoreValue(kvp.Value, isPersonalTrait: true);
                personalTraitsRatings[kvp.Key] = scoreValue;
                Console.WriteLine($"  Criterion {kvp.Key}: RatingId {kvp.Value} → ScoreValue {scoreValue}");
            }

            previousPersonalTraits = new Dictionary<int, int>(ExistingPersonalTraits);

            // ✅ Notify parent immediately after loading
            var ratingsWithRatingIds = ConvertToRatingIds(personalTraitsRatings, isStudentActivity: false);
            OnPersonalTraitsChanged.InvokeAsync(ratingsWithRatingIds);
        }

        // ✅ FIXED: Only reload Student Activities if they changed
        if (ExistingStudentActivities != null && !AreRatingsDictionariesEqual(ExistingStudentActivities, previousStudentActivities))
        {
            Console.WriteLine($"🔄 Loading {ExistingStudentActivities.Count} existing Student Activities ratings");

            studentActivitiesRatings = new Dictionary<int, int>();
            foreach (var kvp in ExistingStudentActivities)
            {
                // RatingId 6-10 maps to ScoreValue 0-4
                int scoreValue = ConvertRatingIdToScoreValue(kvp.Value, isPersonalTrait: false);
                studentActivitiesRatings[kvp.Key] = scoreValue;
                Console.WriteLine($"  Criterion {kvp.Key}: RatingId {kvp.Value} → ScoreValue {scoreValue}");
            }

            previousStudentActivities = new Dictionary<int, int>(ExistingStudentActivities);

            // ✅ Notify parent immediately after loading
            var ratingsWithRatingIds = ConvertToRatingIds(studentActivitiesRatings, isStudentActivity: true);
            OnStudentActivitiesChanged.InvokeAsync(ratingsWithRatingIds);
        }
    }

    private bool AreRatingsDictionariesEqual(Dictionary<int, int>? dict1, Dictionary<int, int>? dict2)
    {
        if (dict1 == null && dict2 == null) return true;
        if (dict1 == null || dict2 == null) return false;
        if (dict1.Count != dict2.Count) return false;

        foreach (var kvp in dict1)
        {
            if (!dict2.TryGetValue(kvp.Key, out var value) || value != kvp.Value)
                return false;
        }
        return true;
    }

    private void UpdateRating(int criterionId, string? value, bool isStudentActivity)
    {
        // ✅ Don't allow updates to Dean-modified criteria
        if (IsDeanModified(criterionId))
        {
            Console.WriteLine($"🚫 Cannot update Dean-modified criterion {criterionId}");
            return;
        }

        var targetDictionary = isStudentActivity ? studentActivitiesRatings : personalTraitsRatings;

        if (int.TryParse(value, out int scoreValue) && scoreValue >= 0)
        {
            targetDictionary[criterionId] = scoreValue;
            Console.WriteLine($"✏️ Updated {(isStudentActivity ? "Student Activity" : "Personal Trait")} Criterion {criterionId} to ScoreValue {scoreValue}");
        }
        else
        {
            targetDictionary.Remove(criterionId);
            Console.WriteLine($"🗑️ Removed {(isStudentActivity ? "Student Activity" : "Personal Trait")} Criterion {criterionId}");
        }

        // Convert ScoreValue to RatingId before sending to parent
        var ratingsWithRatingIds = ConvertToRatingIds(targetDictionary, isStudentActivity);

        Console.WriteLine($"📤 Sending {ratingsWithRatingIds.Count} {(isStudentActivity ? "Student Activity" : "Personal Trait")} ratings to parent");

        if (isStudentActivity)
        {
            OnStudentActivitiesChanged.InvokeAsync(ratingsWithRatingIds);
        }
        else
        {
            OnPersonalTraitsChanged.InvokeAsync(ratingsWithRatingIds);
        }
    }

    private string GetRatingValue(int criterionId, bool isStudentActivity)
    {
        var targetDictionary = isStudentActivity ? studentActivitiesRatings : personalTraitsRatings;
        return targetDictionary.TryGetValue(criterionId, out var scoreValue)
            ? scoreValue.ToString()
            : string.Empty;
    }

    private decimal CalculatePersonalTraitsTotal()
    {
        // 5 criteria × max 2 points = 10
        // Value 0=0, 1=0.5, 2=1.0, 3=1.5, 4=2.0
        return personalTraitsRatings.Values.Sum(v => v * 0.5m);
    }

    private decimal CalculateStudentActivitiesTotal()
    {
        // 3 criteria, scaled to ~10 points
        decimal total = 0;

        foreach (var value in studentActivitiesRatings.Values)
        {
            total += value switch
            {
                0 => 0m,
                1 => 1m,
                2 => 2m,
                3 => 3m,
                4 => 3.33m,
                _ => 0m
            };
        }

        return Math.Round(total, 2);
    }

    // Convert ScoreValue (0-4) to RatingId
    private Dictionary<int, int> ConvertToRatingIds(Dictionary<int, int> scoreValues, bool isStudentActivity)
    {
        var result = new Dictionary<int, int>();

        foreach (var kvp in scoreValues)
        {
            int ratingId;
            if (isStudentActivity)
            {
                // Student Activities: ScoreValue 0-4 maps to RatingId 6-10
                ratingId = kvp.Value + 6;
            }
            else
            {
                // Personal Traits: ScoreValue 0-4 maps to RatingId 11-15
                ratingId = kvp.Value + 11;
            }
            result[kvp.Key] = ratingId;
        }

        return result;
    }

    // Convert RatingId to ScoreValue (0-4) for display
    private int ConvertRatingIdToScoreValue(int ratingId, bool isPersonalTrait)
    {
        if (isPersonalTrait)
        {
            // Personal Traits: RatingId 11-15 maps to ScoreValue 0-4
            return ratingId >= 11 && ratingId <= 15 ? ratingId - 11 : 0;
        }
        else
        {
            // Student Activities: RatingId 6-10 maps to ScoreValue 0-4
            return ratingId >= 6 && ratingId <= 10 ? ratingId - 6 : 0;
        }
    }

    public class GradeOption
    {
        public int Value { get; set; }
        public string Label { get; set; } = string.Empty;
    }
}