@* GradesComponent.razor *@
<div class="card shadow-sm mx-auto" style="max-width: 1100px;">
    <div class="card-body p-4">
        <h5 class="text-end text-dark mb-4">1-2 رابعاً: الأنشطة الطلابية</h5>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط رياضي</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(12, true)"
                        @onchange="e => UpdateRating(12, e.Value?.ToString(), true)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط اجتماعي</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(13, true)"
                        @onchange="e => UpdateRating(13, e.Value?.ToString(), true)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">نشاط ثقافي</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(14, true)"
                        @onchange="e => UpdateRating(14, e.Value?.ToString(), true)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="border-top pt-3">
            <p class="text-end fw-semibold mb-0">
                إجمالي تقييم الأنشطة الطلابية:
                @CalculateStudentActivitiesTotal() / 10
            </p>
        </div>
    </div>
</div>

<div class="card shadow-sm mx-auto mb-4 mt-4" style="max-width: 1100px;">
    <div class="card-body p-4">
        <h5 class="text-end text-dark mb-4">السمات الشخصية</h5>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">التعاون والعمل الجماعي</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(15, false)"
                        @onchange="e => UpdateRating(15, e.Value?.ToString(), false)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">الالتزام بالمواعيد</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(16, false)"
                        @onchange="e => UpdateRating(16, e.Value?.ToString(), false)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">المظهر العام</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(17, false)"
                        @onchange="e => UpdateRating(17, e.Value?.ToString(), false)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">المبادرة و تحمل المسؤولية</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(18, false)"
                        @onchange="e => UpdateRating(18, e.Value?.ToString(), false)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>

        <div class="row mb-3 align-items-center">
            <div class="col-md-8 text-end">
                <label class="form-label mb-0">ادارة الوقت</label>
            </div>
            <div class="col-md-4">
                <select class="form-select"
                        value="@GetRatingValue(19, false)"
                        @onchange="e => UpdateRating(19, e.Value?.ToString(), false)"
                        disabled="@IsViewMode">
                    <option value="">اختر</option>
                    @foreach (var option in gradeOptions)
                    {
                        <option value="@option.Value">@option.Label</option>
                    }
                </select>
            </div>
        </div>
        <div class="border-top pt-3">
            <p class="text-end fw-semibold mb-0">
                إجمالي تقييم الأنشطة الطلابية:
                @CalculateStudentActivitiesTotal() / 10
            </p>
        </div>
    </div>
</div>

@code {
    [Parameter] public EventCallback<Dictionary<int, int>> OnPersonalTraitsChanged { get; set; }
    [Parameter] public EventCallback<Dictionary<int, int>> OnStudentActivitiesChanged { get; set; }
    [Parameter] public bool IsViewMode { get; set; }
    [Parameter] public Dictionary<int, int>? ExistingPersonalTraits { get; set; }
    [Parameter] public Dictionary<int, int>? ExistingStudentActivities { get; set; }

    private Dictionary<int, int> personalTraitsRatings = new();
    private Dictionary<int, int> studentActivitiesRatings = new();

    private List<GradeOption> gradeOptions = new List<GradeOption>
    {
        new GradeOption { Value = 1, Label = "ضعيف" },
        new GradeOption { Value = 2, Label = "مقبول" },
        new GradeOption { Value = 3, Label = "جيد" },
        new GradeOption { Value = 4, Label = "جيد جداً" },
        new GradeOption { Value = 5, Label = "ممتاز" }
    };

    protected override void OnParametersSet()
    {
        if (ExistingPersonalTraits != null && ExistingPersonalTraits.Any())
        {
            personalTraitsRatings = new Dictionary<int, int>(ExistingPersonalTraits);
        }

        if (ExistingStudentActivities != null && ExistingStudentActivities.Any())
        {
            studentActivitiesRatings = new Dictionary<int, int>(ExistingStudentActivities);
        }
    }

    private void UpdateRating(int criterionId, string? value, bool isStudentActivity)
    {
        var targetDictionary = isStudentActivity ? studentActivitiesRatings : personalTraitsRatings;

        if (int.TryParse(value, out int ratingId) && ratingId > 0)
        {
            targetDictionary[criterionId] = ratingId;
        }
        else
        {
            targetDictionary.Remove(criterionId);
        }

        if (isStudentActivity)
        {
            OnStudentActivitiesChanged.InvokeAsync(studentActivitiesRatings);
        }
        else
        {
            OnPersonalTraitsChanged.InvokeAsync(personalTraitsRatings);
        }
    }

    private string GetRatingValue(int criterionId, bool isStudentActivity)
    {
        var targetDictionary = isStudentActivity ? studentActivitiesRatings : personalTraitsRatings;
        return targetDictionary.TryGetValue(criterionId, out var ratingId) ? ratingId.ToString() : "";
    }

    private decimal CalculateStudentActivitiesTotal()
    {
        return studentActivitiesRatings.Values.Sum();
    }

    public class GradeOption
    {
        public int Value { get; set; }
        public string Label { get; set; } = string.Empty;
    }
}